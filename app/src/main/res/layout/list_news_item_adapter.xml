<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    style="@style/Base.V7.Theme.AppCompat.Dialog"
    android:background="@color/colorPrimary">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@mipmap/ic_launcher"
        android:id="@+id/iwLogoNews"
        android:scaleType="fitStart"
        android:minHeight="100dp"
        android:minWidth="100dp"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:background="@color/colorPrimary" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tvDescribe"
        android:layout_below="@+id/tvTitle"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_toRightOf="@+id/iwLogoNews"
        android:layout_toEndOf="@+id/iwLogoNews"
        android:background="@color/colorPrimary"
        android:layout_above="@+id/tvDate"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:textSize="10sp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tvTitle"
        android:textSize="12sp"
        android:background="@color/colorPrimary"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_toRightOf="@+id/iwLogoNews"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:layout_toEndOf="@+id/iwLogoNews" />

    <TextView
        android:text="date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tvDate"
        android:background="@color/colorPrimary"
        android:layout_alignBottom="@+id/iwLogoNews"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_toRightOf="@+id/iwLogoNews"
        android:layout_toEndOf="@+id/iwLogoNews"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:textSize="8dp" />

</RelativeLayout>